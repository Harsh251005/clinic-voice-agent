import { PageHeader } from "@/components/app/page-header";

// Filled in by slice D (new clinic, admins only).
export default function NewClinicPage() {
  return <main className="mx-auto max-w-2xl px-4 py-10"><PageHeader title="Set up a new clinic" /></main>;
}
