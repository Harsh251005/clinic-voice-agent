import { PageHeader } from "@/components/app/page-header";

// Filled in by slice D (clinic setup).
export default function SetupPage() {
  return <PageHeader title="Clinic setup" description="Everything the receptionist says about this clinic comes from these details." />;
}
