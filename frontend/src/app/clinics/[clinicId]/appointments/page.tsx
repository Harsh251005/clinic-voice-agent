import { PageHeader } from "@/components/app/page-header";

// Filled in by slice C (appointments).
export default function AppointmentsPage() {
  return <PageHeader title="Appointments" description="Bookings made on calls appear here as soon as they are confirmed." />;
}
